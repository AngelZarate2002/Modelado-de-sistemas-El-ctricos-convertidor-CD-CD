[t, y] = ode45(@CFEOde, [0 0.01], [0 0]);

figure(1)
subplot(2,1,1)
plot(t, y(:,1));
grid on
title("");
xlabel("");
ylabel("");

subplot(2,1,2)
plot(t, y(:,2));
grid on
title("");
xlabel("");
ylabel("");
